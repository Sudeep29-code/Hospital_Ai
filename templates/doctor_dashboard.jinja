<!DOCTYPE html>
<html>

<head>
    <title>Doctor Dashboard</title>
    <meta http-equiv="refresh" content="15">
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
    <style>
        /* Base Layout */
        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
            background-color: #f5f6f8;
        }

        .doctor-dashboard {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 30px 0;
        }

        /* Header */
        .dashboard-header {
            width: 80%;
            text-align: center;
            margin-bottom: 20px;
        }

        .dashboard-header h1 {
            margin: 0;
            font-size: 26px;
            color: #2c3e50;
        }

        .dashboard-header p {
            margin-top: 5px;
            color: #555;
        }

        /* Card */
        .card {
            background: #ffffff;
            width: 80%;
            margin: 15px 0;
            padding: 25px;
            border-radius: 8px;
            border: 1px solid #e0e0e0;
        }

        /* Section Titles */
        .card h2 {
            margin-bottom: 15px;
            color: #2c3e50;
        }

        /* Tables */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        th {
            background-color: #007bff;
            color: white;
            padding: 10px;
            font-weight: 600;
            font-size: 14px;
        }

        td {
            padding: 10px;
            border-bottom: 1px solid #e5e5e5;
            font-size: 14px;
            text-align: center;
        }

        tr:hover {
            background-color: #f9f9f9;
        }

        /* Regular Button */
        button,
        .complete-btn {
            padding: 6px 14px;
            border-radius: 4px;
            border: none;
            background-color: #007bff;
            color: white;
            font-size: 13px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
        }

        button:hover,
        .complete-btn:hover {
            opacity: 0.9;
        }

        /* Emergency Section Styling */
        .card:nth-of-type(4) h2 {
            color: #dc3545;
        }

        /* Emergency Section Styling */
        .emergency-card h2 {
            color: #dc3545;
        }

        .emergency-card th {
            background-color: #dc3545;
        }

        .emergency-card .complete-btn,
        .emergency-card button {
            background-color: #dc3545;
        }



        /* Badge */
        .badge-green {
            display: inline-block;
            padding: 4px 10px;
            background-color: #28a745;
            color: white;
            border-radius: 4px;
            font-size: 12px;
        }

        /* Responsive */
        @media (max-width: 768px) {

            .card,
            .dashboard-header {
                width: 95%;
            }
        }

        table {
            width: 100%;
            table-layout: fixed;
        }

        th,
        td {
            width: 33.33%;
        }

        .emergency-card {
            font-weight: bold;
        }

        .emergency-card th,
        .emergency-card td {
            font-weight: bold;
        }

        .emergency-card,
        .emergency-card * {
            font-weight: 900 !important;
        }

        /* Priority Badges */
        .priority-high {
            background-color: #dc3545;
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
        }

        .priority-medium {
            background-color: #ffc107;
            color: black;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
        }

        .priority-low {
            background-color: #28a745;
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
        }

        /* Priority Badges */
        .priority-high {
            background-color: #dc3545;
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
        }

        .priority-medium {
            background-color: #ffc107;
            color: black;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
        }

        .priority-low {
            background-color: #28a745;
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
        }

        /* Utilization Progress Bar */
        .progress-bar {
            width: 100%;
            height: 10px;
            background: #e0e0e0;
            border-radius: 6px;
            margin-top: 6px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: #007bff;
            border-radius: 6px;
            transition: width 0.5s ease-in-out;
        }

        @keyframes pulse {
            0% {
                box-shadow: 0 0 0 0 rgba(220, 53, 69, 0.7);
            }

            70% {
                box-shadow: 0 0 0 12px rgba(220, 53, 69, 0);
            }

            100% {
                box-shadow: 0 0 0 0 rgba(220, 53, 69, 0);
            }
        }

        .emergency-card h2::before {
            content: "";
            display: inline-block;
            width: 10px;
            height: 10px;
            background-color: #dc3545;
            border-radius: 50%;
            margin-right: 10px;
            animation: pulse 1.5s infinite;
        }
    </style>


</head>

<body>

    <div class="doctor-dashboard">

        <div class="dashboard-header">
            <h1>Welcome Dr. {{ doctor_name }}</h1>
            <p>Department: {{ department }}</p>
        </div>

        <div class="card">
            <h2>📊 Doctor Analytics</h2>
            <p>Average Consultation Time: {{ avg_time }} mins</p>
            <p>Utilization: {{ utilization }}%</p>
            <div class="progress-bar">
                <div class="progress-fill" style="width: {{ utilization | default(0) | int }}%;">
                </div>
            </div>


        </div>

        <div class="card">
            <h2>🤖 AI Optimization Status</h2>
            <p>Fairness Index: {{ fairness_index }}
            </p>
            <p>Optimization Score: {{ optimization_score }}
                /100</p>
            {% if system_status == "Stable" %}
            <span class="badge badge-green">System Stable</span>
            {% elif system_status == "Moderate Load" %}
            <span class="badge" style="background:#ffc107;color:black;">Moderate Load</span>
            {% else %}
            <span class="badge" style="background:#dc3545;">System Overloaded</span>
            {% endif %}

        </div>

        <div class="card">
            <h2> 🟢 Regular Queue ({{ regular_patients|length }})</h2>

            <table>
                <thead>
                    <tr>
                        <th>Patient Name</th>
                        <th>Priority</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    {% for patient in regular_patients %}
                    <tr>
                        <td>{{ patient.name }}</td>

                        <td>
                            <span class="priority-{{ patient.priority|lower }}">
                                {{ patient.priority }}
                            </span>
                        </td>

                        <td>
                            <a href="{{ url_for('complete_patient', patient_id=patient.id) }}" class="complete-btn">
                                Completed
                            </a>
                        </td>

                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>


        <div class="card emergency-card">
            <h2>Emergency Queue ({{ emergency_patients|length }})</h2>


            <table>
                <thead>
                    <tr>
                        <th>Patient Name</th>
                        <th>Priority</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    {% for patient in emergency_patients %}
                    <tr>
                        <td>{{ patient.name }}</td>
                        <td>
                            <span class="priority-{{ patient.priority|lower }}">
                                {{ patient.priority }}
                            </span>
                        </td>
                        <td class="action-cell">
                            <a href="{{ url_for('complete_patient', patient_id=patient.id) }}" class="complete-btn">
                                Completed
                            </a>
                        </td>


                    </tr>
                    {% endfor %}
                </tbody>
            </table>

        </div>


    </div>

</body>


</html>